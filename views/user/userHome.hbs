<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Home Page</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    /* Body background color */
    body {
      background-color: #f8f9fa;
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    /* Navbar Styles */
    .navbar {
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .navbar-brand {
      font-size: 1.6rem;
      font-weight: bold;
    
     color: white;
    }

    .navbar-dark .navbar-nav .nav-link {
      color: #f8f9fa;
    }

    .navbar-nav .nav-link:hover {
      color: #ffac00;
    }

    /* Main Content */
    .container {
      background-color: #ffffff;
      padding: 40px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      max-width: 700px;
      margin-top: 60px;
      text-align: center;
      flex-grow: 1;
    }

    .container h2 {
      color: black;
      font-size: 2rem;
      font-weight: 600;
    }

    .lead {
      color: #6c757d;
      font-size: 1.2rem;
      margin-top: 20px;
    }

    /* Custom Spacer */
    .spacer {
      height: 100px;
    }

    /* Footer */
    footer {
      background-color:#060270; /* Changed footer to a blue color */
      color: #fff;
      font-size: 0.9rem;
      padding: 10px 0;
      text-align: center;
      margin-top: auto;
    }

    footer pre {
      margin: 0;
    }

    /* Logout Button */
    .btn-danger {
      background-color: #dc3545;
      border: none;
      font-weight: bold;
      padding: 10px 20px;
      border-radius: 5px;
    }

    .btn-danger:hover {
      background-color: #c82333;
      color: white;
    }
  </style>
</head>

<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg" style="background-color: #060270;">

    <div class="container-fluid">
      <a class="navbar-brand" href="#">Home Page</a>
      <div class="d-flex">
        <a href="/user/logout" class="btn btn-danger">Logout</a>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container">
    <h2 class="mb-3">Welcome {{username}}!</h2>
    <p class="lead">Congratulations! You are successfully logged in.</p>
  </div>

  <div class="spacer"></div>

  <!-- Footer -->
  <footer>
    <pre>© 2024 anagha All rights reserved.</pre>
  </footer>

  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>
